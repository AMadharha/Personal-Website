services:
  app:
    build:
      context: .
      target: dev
    environment:
      CHOKIDAR_USEPOLLING: 'true'
    ports:
      - '5173:5173'
    volumes:
      - .:/app
      - node_modules:/app/node_modules

volumes:
  node_modules: